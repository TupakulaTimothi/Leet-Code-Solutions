class Solution(object):
    def smallerNumbersThanCurrent(self, nums):
        l=[]
        for i in nums:
            c=0
            for j in nums:
                if i>j:
                    c+=1
            l.append(c)
        return l
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        
