class Solution(object):
    def countBits(self, n):
        ans=[]
        for i in range(n+1):
            c=0
            while i:
                c+=i%2
                i/=2
            ans.append(c)
        return ans
        """
        :type n: int
        :rtype: List[int]
        """
        
